/**
 * The design system is held to the token rules more strictly than a screen,
 * not less: a dead utility here breaks every app at once.
 */
module.exports = require("@cfc/config/eslint-package");
